document.getElementById('contactForm').addEventListener('submit', function (event) { event.preventDefault(); const name = document.getElementById('name').value; const email = document.getElementById('email').value; const password = document.getElementById('password').value; const date = document.getElementById('date').value; const gender = document.getElementById('gender').value; const formData = { name: name, email: email, password: password, date: date, gender: gender }; const formDataString = JSON.stringify(formData, null, 2); const blob = new Blob([formDataString], { type: 'application/json' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.style.display = 'none'; a.href = url; a.download = 'formData.json'; document.body.appendChild(a); a.click(); URL.revokeObjectURL(url); });